---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rustfs-data
  namespace: rustfs
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: rustfs-data
  resources:
    requests:
      storage: 460Gi
